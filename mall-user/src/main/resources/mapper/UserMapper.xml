<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.user.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.mall.user.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="nickname" property="nickname"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="status" property="status"/>
        <result column="role" property="role"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, nickname, phone, email, avatar, status, role, created_at, updated_at)
        VALUES (#{username}, #{nickname}, #{phone}, #{email}, #{avatar}, #{status}, #{role}, #{createdAt}, #{updatedAt})
    </insert>
    
    <update id="update">
        UPDATE user 
        SET nickname = #{nickname}, 
            phone = #{phone}, 
            email = #{email}, 
            avatar = #{avatar}, 
            status = #{status}, 
            role = #{role},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="updateAvatar">
        UPDATE user SET avatar = #{avatar}, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>